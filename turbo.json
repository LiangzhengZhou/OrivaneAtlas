{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/docs/api/openapi.json"],
      "outputs": ["dist/**"]
    },
    "typecheck": { "dependsOn": ["^typecheck"], "outputs": [] }
  }
}
